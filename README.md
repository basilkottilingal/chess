# chess

A simple chess engine in C

## play with the bot

If you have a linux system (as POSIX is required), with *python3* and *python http.server*,
you can play with the bot using the following bash script
~~~bash
chmod +x ./launch
./launch
~~~
